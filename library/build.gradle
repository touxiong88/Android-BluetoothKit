apply plugin: 'com.android.library'
apply plugin: 'com.jfrog.bintray'

version="1.4.0"

android {
    namespace "com.faytech.bluetooth.library"
    compileSdkVersion 27


    resourcePrefix "bluetoothkit_"

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            archivesBaseName = "FaytechScannerlibrary"
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            archivesBaseName = "FaytechScanneribrary"
        }
    }
}
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}


def siteUrl = 'https://github.com/dingjikerbo/BluetoothKit'
def gitUrl = 'https://github.com/dingjikerbo/BluetoothKit.git'
group = "com.faytech.bluetooth" // Maven Group ID for the artifact

task sourcesJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
//    classifier = 'sources' // Set the classifier here
}

Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())
bintray {
    user = properties.getProperty("bintray.user")
    key = properties.getProperty("bintray.apikey")
    configurations = ['archives']
    pkg {
        repo = "maven"
        name = "BluetoothKit"
        websiteUrl = siteUrl
        vcsUrl = gitUrl
        licenses = ["Apache-2.0"]
        publish = true
    }
}